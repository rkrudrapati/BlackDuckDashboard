This repository contains the automations that we use daily.
The tokens and urls used are not valid. Please replace them with appropriate values before running the scripts.

Requirements:
requests
json
csv
